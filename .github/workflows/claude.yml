name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      actions: read # Required for Claude to read CI results on PRs
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          # Workload Identity Federation (静的 API キー不使用)
          anthropic_federation_rule_id: "fdrl_01FkxzE14KLG8kANGWfeVNEp"
          anthropic_organization_id: "8efe77da-6956-4af6-968f-24a2e7e4e120"
          anthropic_service_account_id: "svac_01FMVZrzTYXJvhtpxJbqJdGP"
          anthropic_workspace_id: "wrkspc_01RTpcRnPUQPaeme8eLvLyoH"
          claude_args: "--model claude-opus-4-8"
          allowed_bots: "release-manager-actions"
          additional_permissions: |
            actions: read

